# raw/google-doc/

Documents exported from Google Docs (and Google Sheets/Slides if relevant).

**Where to export from:**
- Google Docs → File → Download → choose Markdown (.md), Plain text (.txt), or Word (.docx)
- For many docs at once: Google Drive → select files → right-click → Download (bundles as a `.zip`)

Drop the downloaded files here.
