# raw/notion/

Pages and databases exported from Notion.

**Where to export from:**
- Notion → open the page or database → "..." menu → Export → choose "Markdown & CSV" (recommended, preserves structure and sub-pages as a folder tree)
- For a whole workspace: Settings → Settings & members → Export content

Drop the exported `.zip` contents (or unzipped folders) here.
